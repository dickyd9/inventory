import { FilterQueryMiddleware } from './filter-query.middleware';

describe('FilterQueryMiddleware', () => {
  it('should be defined', () => {
    expect(new FilterQueryMiddleware()).toBeDefined();
  });
});
